---
title: insler.dev
description: A family of typed, contract-first TypeScript subsystems for building services.
template: splash
hero:
  title: The @insler family
  tagline: Typed, contract-first building blocks for TypeScript services — pick one subsystem or compose them all.
editUrl: false
lastUpdated: false
---

import SubsystemGrid from '../../components/SubsystemGrid.astro';

## The subsystems

Each subsystem is its own product — one umbrella package on npm, its own docs
site, one shared family identity. Start anywhere; they compose, but none of
them require each other.

<SubsystemGrid />
